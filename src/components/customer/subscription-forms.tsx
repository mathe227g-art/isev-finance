"use client";
import { ActionForm, ConfirmAction } from "@/components/finance/forms";
import { saveSubscription, cancelSubscription } from "@/app/actions/customer";
import { frequencies, today } from "@/lib/finance";
import type { Account, Category } from "@/types/finance";
export function SubscriptionForm({profileId,accounts,categories}:{profileId:string;accounts:Account[];categories:Category[]}) {return <ActionForm action={saveSubscription} profileId={profileId} submit="Adicionar assinatura"><label>Serviço ou plano<input name="description" required minLength={2} maxLength={160} placeholder="Ex.: Streaming família"/></label><label>Fornecedor<input name="merchant_name" maxLength={100} placeholder="Ex.: Netflix"/></label><div className="form-grid"><label>Valor<input name="amount" required inputMode="decimal" placeholder="49,90"/></label><label>Frequência<select name="frequency" defaultValue="monthly">{Object.entries(frequencies).map(([value,label])=><option value={value} key={value}>{label}</option>)}</select></label><label>Dia da cobrança<input name="due_day" type="number" min={1} max={31} defaultValue={10} required/></label><label>Avisar com antecedência<input name="renewal_notice_days" type="number" min={0} max={60} defaultValue={7} required/></label></div><label>Conta<select name="account_id" required defaultValue=""><option value="" disabled>Selecione</option>{accounts.map((a)=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label><label>Categoria<select name="category_id" required defaultValue=""><option value="" disabled>Selecione</option>{categories.filter((c)=>c.kind==="expense").map((c)=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Início<input name="start_date" type="date" required defaultValue={today()}/></label><label>Site do serviço (opcional)<input name="service_url" type="url" maxLength={500} placeholder="https://..."/></label><label>Observações<textarea name="notes" maxLength={2000}/></label></ActionForm>}
export function SubscriptionCancel({profileId,id,description}:{profileId:string;id:string;description:string}) {return <ConfirmAction profileId={profileId} id={id} action={cancelSubscription} label="Cancelar" message={`Cancelar “${description}”? O histórico será mantido e previsões pendentes futuras serão canceladas.`}/>}
